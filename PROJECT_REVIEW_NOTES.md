# 项目审查说明

## 实际实现的页面

- `/login`：登录 / 注册，小在主视觉与低压力文案
- `/home`：首页，日期问候、小在、入口、记录/聊天/守护人数统计
- `/chat`：和小在聊聊，文字输入、示例场景、结构化确认卡、Safety 响应
- `/records`：心情历史，编辑、删除、标签与风险等级展示
- `/letter`：每周来信，信纸阅读体验、趋势图、情绪分布、成长相册
- `/guardians`：守护圈，添加守护人、逐项授权、模拟通知审计
- `/privacy`：隐私中心，资料修改、守护范围、AI 记忆、退出/注销
- API Routes：认证、聊天、记录、周信、守护人、通知、记忆、个人资料

## 实际实现的 AI 工作流

- 陪伴回复：低/中风险路径先温和追问，再生成结构化记录；高风险绕过普通聊天
- 结构化情绪抽取：情绪标签、强度、触发事件、想法、身体/行为反应、非诊断摘要
- 每周来信：聚合最近 7 天（数据少时回退 30 天），生成趋势、洞察、下周小约定与成长节点
- 风险分级：固定规则检测 High / Medium / Low，并进入对应路由
- Safety Workflow：固定、受控的安全响应，不依赖普通聊天 Prompt 自由处理

## Mock / 模拟的功能

- LLM 调用：当前未配置真实 API Key，使用离线确定性引擎替代
- 语音输入：仅 UI 入口，没有真实语音转文字
- 守护通知：站内模拟，不接入真实短信 / Push / 微信
- 守护人视角：在本地用授权字段模拟展示，不涉及独立守护人客户端

## 守护圈通知是真实发送还是站内模拟

守护圈通知是**站内模拟**。系统会创建 `notification_events` 记录，支持“待确认 / 已发送 / 已撤销”状态与审计字段，但不会发送真实短信、微信或第三方 IM 消息。

## 风险分级逻辑是规则实现还是 LLM 实现

当前版本是**规则实现**。`src/lib/risk.ts` 通过固定 High / Medium / Low 关键词与风险路由判断。项目预留了 LLM 配置，但为了本地稳定演示，未配置时不会依赖 LLM。

## 当前数据库是否真实写入

是。当前版本使用 SQLite，数据库文件为 `data/youwozai.db`。用户、会话、情绪记录、每周来信、守护人、授权、通知审计和 AI 记忆都会真实写入。

## 当前版本最需要人工检查的地方

- 登录认证与会话 Cookie 的安全策略（当前为本地 MVP，生产环境应接入成熟认证服务）
- 高风险通知的撤销/发送策略是否满足目标地区法律、隐私、安全和临床治理要求
- 高风险安全文案是否适合真实上线，不能替代紧急服务
- 守护圈授权矩阵的默认值与 UI 是否足够清晰、可撤销
- LLM 接入后的结构化 JSON 校验、重试和降级策略
- 生产构建在当前 Windows 环境下 `spawn UNKNOWN` 的问题，需要确认目标部署环境
