# 有我在项目协作规则

## 项目目标

维护一个可运行、可验证、尊重用户控制权的 AI 陪伴与情绪记录 MVP。它不是医疗诊断或紧急救援产品。

## 开始任务前

1. 阅读 `README.md`、`docs/PRD.md` 和 `docs/SPEC.md`。
2. High Risk、守护圈或通知相关变更必须阅读 `docs/SAFETY.md`。
3. 语音、Provider 或浏览器播放变更必须阅读 `docs/TESTING.md`。
4. 明确需求编号、非目标和验收标准后再改代码。

## 不可破坏的约束

- High Risk 固定安全流程优先于生成式回复。
- 未经用户事先授权和确认，不得向守护人分享信息。
- AI 抽取结果必须由用户确认后保存。
- LLM 或 TTS 失败不得阻断文字聊天。
- 任何时候最多播放一条小在语音，切页必须停止。
- 密钥只能放在未提交的环境变量文件中，不得写入源码、日志或文档。
- 不得把本地模拟能力描述为真实线上能力。

## 工程要求

- 保持 API、领域逻辑和数据访问的现有分层。
- 优先做小而可验证的修改，不重写无关模块。
- 新业务行为必须补测试；修复回归必须先覆盖失败场景。
- 完成前至少运行 `pnpm test` 和 `pnpm build`。
- 浏览器相关变更还要按 `docs/TESTING.md` 做人工验收。
- 更新 `docs/CHANGELOG.md` 和已知问题。

## 交付报告

说明真实原因、修改文件、测试结果、构建结果、外部限制和剩余问题。不得把未运行的验证写成已通过。
